import os
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.metrics import confusion_matrix

# True labels and predictions from final test evaluation
y_true = [
    # We'll replace this with the actual values
]